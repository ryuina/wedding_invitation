import styled from "styled-components";

export const Title = styled.p`
  font-size: 1.2rem;
  color: var(--title-color);
  font-weight: bold;
  margin-bottom: 0;
  text-align: center;

  span {
    font-size: 1rem;
  }
`;